[tool.ruff]
target-version = "py312"

[tool.ruff.lint]
# Errors, undefined names, unused imports/variables. Style rules are not
# enforced; the docstrings are the style.
select = ["E4", "E7", "E9", "F"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
